# einvoice-models-nic

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build einvoice-models-nic` to build the library.

## Running unit tests

Run `nx test einvoice-models-nic` to execute the unit tests via [Jest](https://jestjs.io).
