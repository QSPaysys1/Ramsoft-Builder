# shared/session (documentation)

Angular session access is implemented in **`@ramsoft-builder/gstr2b/data-access/services`** as `Gstr2bSessionConsumer`.

Refer GSTR-1 authentication/session establishment flow (`Gstr1AuthStore`, `/gstr1/login`).
