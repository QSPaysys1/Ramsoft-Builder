# gstr2b-models-enums

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gstr2b-models-enums` to build the library.

## Running unit tests

Run `nx test gstr2b-models-enums` to execute the unit tests via [Jest](https://jestjs.io).
