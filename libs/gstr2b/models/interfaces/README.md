# gstr2b-models-interfaces

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gstr2b-models-interfaces` to build the library.

## Running unit tests

Run `nx test gstr2b-models-interfaces` to execute the unit tests via [Jest](https://jestjs.io).
