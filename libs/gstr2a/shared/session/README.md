# gstr2a-shared-session

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gstr2a-shared-session` to build the library.

## Running unit tests

Run `nx test gstr2a-shared-session` to execute the unit tests via [Jest](https://jestjs.io).
