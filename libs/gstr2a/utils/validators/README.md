# gstr2a-utils-validators

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gstr2a-utils-validators` to build the library.

## Running unit tests

Run `nx test gstr2a-utils-validators` to execute the unit tests via [Jest](https://jestjs.io).
