# gstr2a-utils-constants

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gstr2a-utils-constants` to build the library.

## Running unit tests

Run `nx test gstr2a-utils-constants` to execute the unit tests via [Jest](https://jestjs.io).
