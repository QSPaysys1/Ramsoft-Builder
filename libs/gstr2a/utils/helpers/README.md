# gstr2a-utils-helpers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gstr2a-utils-helpers` to build the library.

## Running unit tests

Run `nx test gstr2a-utils-helpers` to execute the unit tests via [Jest](https://jestjs.io).
