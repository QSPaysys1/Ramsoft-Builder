# ewaybills-models-ewb

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ewaybills-models-ewb` to build the library.

## Running unit tests

Run `nx test ewaybills-models-ewb` to execute the unit tests via [Jest](https://jestjs.io).
