# ewaybills-utils-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ewaybills-utils-core` to build the library.

## Running unit tests

Run `nx test ewaybills-utils-core` to execute the unit tests via [Jest](https://jestjs.io).
