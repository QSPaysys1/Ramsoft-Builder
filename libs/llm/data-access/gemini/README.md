# llm-data-access-gemini

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build llm-data-access-gemini` to build the library.

## Running unit tests

Run `nx test llm-data-access-gemini` to execute the unit tests via [Jest](https://jestjs.io).
